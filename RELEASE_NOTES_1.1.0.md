# Command Widget 1.1.0

This release packages the complete Plasma 6 widget, telemetry collectors,
loopback HTTP service, device discovery, and configuration UI for installation
through Command Centre and the SourceForge CommandOS repository.

Install from Command Centre under **Command Apps**, or run `sudo pacman -S
command-widget` followed by `command-widget-install`.
